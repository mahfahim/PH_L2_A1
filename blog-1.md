# Title: TypeScript-এ Type Safety: any এবং unknown-এর মধ্যে পার্থক্য ও Type Narrowing-এর গুরুত্ব



## Introduction
টাইপস্ক্রিপ্ট (TypeScript) ব্যবহারের প্রাথমিক উদ্দেশ্য হলো কোডকে আরও বেশি সুরক্ষিত এবং বাগ-মুক্ত রাখা। তবে অনেক সময় এপিআই রেসপন্স বা ইউজার ইনপুট থেকে এমন ডাটা আসে যার টাইপ আমাদের আগে থেকে জানা থাকে না। এই অনিশ্চিত ডাটা হ্যান্ডেল করার জন্য টাইপস্ক্রিপ্টে `any` এবং `unknown` নামে দুটি টাইপ রয়েছে। যদিও অনেকে দ্রুত কাজ সারার জন্য `any` ব্যবহার করেন, কিন্তু এটি কোডের টাইপ সেফটিকে ঝুঁকির মুখে ফেলে দেয়। আজকের ব্লগে আমরা আলোচনা করব কেন `any` কে একটি টাইপ সেফটি হোল বলা হয় এবং কেন `unknown` ব্যবহার করা একজন প্রফেশনাল ডেভেলপারের জন্য সঠিক পদ্ধতি।



## Body Paragraphs with Code Snippets

### any: টাইপ সেফটি সিস্টেমের একটি ছিদ্র
যখন আমরা কোনো ভেরিয়েবলের টাইপ `any` দিয়ে দেই, তখন টাইপস্ক্রিপ্ট সেই ভেরিয়েবলের জন্য আর কোনো টাইপ চেকিং করে না। এটি মূলত টাইপস্ক্রিপ্টের সুরক্ষা কবজকে বন্ধ করে দেওয়ার মতো। এর ফলে কোডটি টাইপস্ক্রিপ্টে লেখা হলেও তা জাভাস্ক্রিপ্টের মতো আচরণ করে, যা রানটাইমে মারাত্মক এরর তৈরি করতে পারে।

**কোড উদাহরণ:**

```typescript
let myData: any = "Hello World";

// ভুল মেথড কল করা হচ্ছে যা রানটাইমে এরর দিবে
// কিন্তু any ব্যবহারের কারণে টাইপস্ক্রিপ্ট কোনো সতর্কতা দিচ্ছে না
console.log(myData.toLowercase()); 

// Result: Runtime Error: myData.toLowercase is not a function
```

এখানে `toLowercase` এর পরিবর্তে `toLowerCase` হওয়ার কথা ছিল। `any` ব্যবহারের কারণে কম্পাইল টাইমে এই ভুলটি ধরা পড়েনি, যা বড় প্রজেক্টের ক্ষেত্রে বিপজ্জনক হতে পারে।

### unknown: ডাটা হ্যান্ডলিংয়ের নিরাপদ বিকল্প
`unknown` টাইপটিও যেকোনো ধরনের ডাটা গ্রহণ করতে পারে, কিন্তু এটি `any`-এর তুলনায় অনেক বেশি কঠোর। `unknown` টাইপের কোনো ভেরিয়েবলের ওপর সরাসরি কোনো অপারেশন বা মেথড চালানো যায় না। এটি ব্যবহারের আগে টাইপস্ক্রিপ্ট আপনাকে নিশ্চিত হতে বাধ্য করে যে ডাটাটি আসলে কী ধরনের।

**কোড উদাহরণ:**

```typescript
let apiResponse: unknown = "New Update";

// সরাসরি মেথড কল করলে এরর আসবে
// console.log(apiResponse.length); // Error: Object is of type 'unknown'.

// এটি ব্যবহারের জন্য টাইপ চেক করা বাধ্যতামূলক
if (typeof apiResponse === "string") {
    console.log(apiResponse.length); // এটি এখন নিরাপদ
}
```

### Type Narrowing: টাইপ সুনির্দিষ্ট করার প্রক্রিয়া
`unknown` বা ডাইনামিক টাইপের ডাটাকে যখন আমরা লজিক্যাল কন্ডিশন ব্যবহার করে একটি নির্দিষ্ট টাইপে (যেমন- string, number বা object) সীমাবদ্ধ করি, তখন তাকে বলা হয় **Type Narrowing**। এটি কোডকে প্রেডিক্টেবল করে তোলে এবং রানটাইম ক্রাশ হওয়ার ঝুঁকি পুরোপুরি কমিয়ে দেয়।

**কোড উদাহরণ:**

```typescript
function printValue(value: unknown) {
    if (typeof value === "number") {
        // এখানে value-এর টাইপ Narrow হয়ে number হয়ে গেছে
        console.log(value.toFixed(2));
    } else if (typeof value === "string") {
        // এখানে value-এর টাইপ Narrow হয়ে string হয়ে গেছে
        console.log(value.toUpperCase());
    } else {
        console.log("Unknown data type");
    }
}
```



## Conclusion
পরিশেষে বলা যায়, `any` টাইপটি টাইপস্ক্রিপ্টের আসল সুবিধাকে নষ্ট করে দেয় এবং কোডে টাইপ সেফটি হোল তৈরি করে। অন্যদিকে, `unknown` ডেভেলপারকে দায়িত্বশীলভাবে কোড লিখতে সাহায্য করে। প্রফেশনাল ডেভেলপমেন্টে অনিশ্চিত ডাটা হ্যান্ডেল করার জন্য `unknown` ব্যবহার করা এবং পরবর্তীতে Type Narrowing-এর মাধ্যমে তা সুনির্দিষ্ট করা একটি উত্তম অভ্যাস। এতে কোড যেমন ক্লিন থাকে, তেমনি অ্যাপ্লিকেশনের স্থায়িত্বও বৃদ্ধি পায়। তাই নিরাপদ কোডিংয়ের স্বার্থে `any` এড়িয়ে `unknown` ব্যবহার করাই বুদ্ধিমানের কাজ।
  
