# Title: TypeScript Generics: রিইউজেবল এবং টাইপ-সেফ কোড তৈরির সহজ গাইড



## Introduction
টাইপস্ক্রিপ্টে কোড করার সময় আমাদের প্রায়ই এমন কিছু ফাংশন তৈরি করতে হয় যা বিভিন্ন ধরনের ডাটার সাথে কাজ করতে পারে। যেমন—কখনো আমাদের একটি ফাংশন স্ট্রিং (string) রিটার্ন করে, আবার কখনো নাম্বার (number)। যদি আমরা প্রতিটি টাইপের জন্য আলাদা আলাদা ফাংশন লিখি, তবে কোড অনেক বড় হয়ে যায়। আবার `any` ব্যবহার করলে টাইপ সেফটি নষ্ট হয়। এই সমস্যার সমাধান হলো Generics। এটি এমন একটি টুল যা আমাদের ফ্লেক্সিবল এবং রিইউজেবল কোড লিখতে সাহায্য করে, যেখানে ডাটার টাইপ যাই হোক না কেন, টাইপস্ক্রিপ্ট তার সুরক্ষা বজায় রাখে।



## Body Paragraphs with Code Snippets

### Generics কী এবং এটি কীভাবে কাজ করে?
Generics-কে সহজভাবে একটি "টাইপ ভেরিয়েবল" হিসেবে ভাবা যেতে পারে। এটি ফাংশন বা ক্লাসের ভেতর টাইপকে একটি ভেরিয়েবল হিসেবে পাস করতে দেয়। সাধারণত একে `<T>` দিয়ে প্রকাশ করা হয়। যখন আমরা ফাংশনটি কল করি, তখন টাইপস্ক্রিপ্ট বুঝে নেয় যে `T`-এর জায়গায় কোন টাইপটি বসবে।

**সহজ কোড উদাহরণ:**

ধরা যাক, আমাদের একটি ফাংশন প্রয়োজন যা ইনপুট হিসেবে যা পাবে, আউটপুট হিসেবে ঠিক তাই ফেরত দিবে।

```typescript
// এখানে T একটি প্লেসহোল্ডার বা টাইপ ভেরিয়েবল
function getInfo<T>(value: T): T {
    return value;
}

// যখন আমরা number পাস করি, T তখন number হয়ে যায়
let age = getInfo<number>(25); 

// যখন আমরা string পাস করি, T তখন string হয়ে যায়
let userName = getInfo<string>("Rahim");

console.log(age.toFixed(1)); // এটি সঠিক কারণ age একটি number
// console.log(userName.toFixed(1)); // এখানে এরর আসবে কারণ string-এ toFixed নেই
```

এই উদাহরণের মাধ্যমে দেখা যাচ্ছে যে, একই ফাংশন `getInfo` বিভিন্ন ধরনের ডাটা নিয়ে কাজ করছে কিন্তু প্রতিবারই সঠিক টাইপটি ধরে রাখছে।

### Generics কেন `any`-এর চেয়ে ভালো?
`any` ব্যবহার করলে টাইপস্ক্রিপ্ট চেক করা বন্ধ করে দেয় যে ভ্যালুটি আসলে কী। ফলে ভুল হওয়ার সম্ভাবনা বাড়ে। কিন্তু Generics ব্যবহার করলে ইনপুট এবং আউটপুটের মধ্যে টাইপের সম্পর্ক বজায় থাকে।

**সহজ কোড উদাহরণ:**

```typescript
function wrapInArray<T>(item: T): T[] {
    return [item];
}

const names = wrapInArray<string>("TypeScript"); // এটি string[] তৈরি করবে
const scores = wrapInArray<number>(100);          // এটি number[] তৈরি করবে

names[0].toUpperCase(); // এটি কাজ করবে
// scores[0].toUpperCase(); // এরর দিবে, কারণ number-এর তো toUpperCase নেই!
```

### অবজেক্ট এবং ইন্টারফেসে Generics-এর ব্যবহার
Generics শুধু ফাংশনে নয়, ইন্টারফেসেও ব্যবহার করা যায়। এটি ডাটা স্ট্রাকচারকে আরও ডাইনামিক করে তোলে।

**সহজ কোড উদাহরণ:**

```typescript
interface Box<T> {
    content: T;
}

let stringBox: Box<string> = { content: "Hello" };
let numberBox: Box<number> = { content: 123 };
```

এখানে `Box` ইন্টারফেসটি রিইউজেবল। আমরা চাইলে এর ভেতরে যেকোনো ধরনের ডাটা রাখতে পারি এবং টাইপস্ক্রিপ্ট সবসময় তার সঠিকতা যাচাই করবে।



## Conclusion
পরিশেষে, Generics হলো টাইপস্ক্রিপ্টের একটি শক্তিশালী ফিচার যা আমাদের কোডকে ছোট, পরিষ্কার এবং রিইউজেবল করতে সাহায্য করে। এটি ব্যবহার করলে আমাদের বারবার একই লজিক আলাদা আলাদা টাইপের জন্য লিখতে হয় না। সবচেয়ে বড় সুবিধা হলো, এটি ফ্লেক্সিবল হওয়ার পাশাপাশি টাইপ সেফটি নিয়ে কোনো আপস করে না। তাই প্রফেশনাল ডেভেলপমেন্টে কোডকে আরও স্মার্ট করতে Generics-এর ব্যবহার অপরিহার্য।
